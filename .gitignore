# macOS
.DS_Store
.AppleDouble
.LSOverride

# Vim runtime state
.viminfo

# Zsh session state
.zsh_sessions/

# Claude Code runtime data (sensitive / machine-specific)
.claude.json
claude/.claude/projects/
claude/.claude/sessions/
claude/.claude/history.jsonl
claude/.claude/cache/
claude/.claude/todos/
claude/.claude/memory/

# Git local identity (name/email — set manually per machine)
.gitconfig.local

# Stow metadata
.stow-local-ignore
tmux/.config/tmux/plugins/
