# macOS
.DS_Store
.AppleDouble
.LSOverride

# Vim runtime state
.viminfo

# Zsh session state
.zsh_sessions/

# Claude Code config (may contain tokens/settings)
.claude.json

# Git local identity (name/email — set manually per machine)
.gitconfig.local

# Stow metadata
.stow-local-ignore
tmux/.config/tmux/plugins/
